<% content_for :title, "Projects" %>

<h1>Projects</h1>

<div id="Projects">
  <% @projects.each do |project| %>
    <%= render project %>
  <% end %>
</div>

<%= link_to "New project", new_project_path %>
